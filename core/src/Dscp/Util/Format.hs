-- | Time primitives with emulation support.

module Dscp.Util.Format
    ( -- * Re-exports
      build
    , genericF
    , listF
    , mapF
    , (+|)
    , (|+)
    ) where

import Fmt (build, genericF, listF, mapF, (+|), (|+))
